interface:
  display_name: "Archive Commit"
  short_description: "Archive verified plan slices in detailed Git history"
  default_prompt: "Use $archive-commit to archive this verified slice in a detailed Conventional Commit and prune completed plan context safely."
